import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  images: {
    domains: ['existing-domain.com', 'coin-images.coingecko.com'],
  },
}


export default nextConfig;
